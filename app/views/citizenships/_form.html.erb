<%= form_for(@citizenship) do |f| %>
  <% if @citizenship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@citizenship.errors.count, "error") %> prohibited this citizenship from being saved:</h2>

      <ul>
      <% @citizenship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country_name %><br>
    <%= f.text_field :country_name %>
  </div>
  <div class="field">
    <%= f.label :origin_citizen %><br>
    <%= f.text_field :origin_citizen %>
  </div>
  <div class="field">
    <%= f.label :origin_citizen_for %><br>
    <%= f.text_field :origin_citizen_for %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
